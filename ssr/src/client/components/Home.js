import React from 'react';

const Home = () => {
  return (
  <div>
    I'm the best best fd home component
    <button onClick={() => console.log('Hi there')}>Press me!</button>
  </div>);
};

export default Home;
